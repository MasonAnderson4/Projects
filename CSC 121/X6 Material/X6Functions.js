		function the_sum_function(x,y){
			z = x + y;
			return z;
		}

			function the_diff_function(x,y){
			z = x - y;
			return z;
		}

			function the_multi_function(x,y){
			z = x * y;
			return z;
		}

		function the_div_function(x,y){
			z = x / y;
			return z;
		}